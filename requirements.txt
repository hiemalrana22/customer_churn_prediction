# Production dependencies for the Vercel function. Keep notebooks, plotting,
# and dataset-download tools out of this file: Vercel installs every entry into
# the serverless-function bundle.
# data + modelling
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.6.0
xgboost>=2.0.0,<3.0.0

# Vercel serverless app
Flask>=3.0.0
